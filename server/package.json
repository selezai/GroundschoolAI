{
  "name": "groundschool-ai-server",
  "version": "1.0.0",
  "description": "Backend server for GroundSchool AI application",
  "main": "dist/index.js",
  "scripts": {
    "start": "node dist/index.js",
    "dev": "nodemon --exec ts-node src/index.ts",
    "build": "cd .. && npm install && npm install -g expo-cli && NODE_ENV=production npx expo export:web && cd server && npm install && tsc && cd .. && mkdir -p server/dist/web-build && cp -r web-build/* server/dist/web-build/",
    "test": "jest"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.18.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "node-fetch": "^3.3.2",
    "openai": "^4.24.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.4",
    "nodemon": "^3.0.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
